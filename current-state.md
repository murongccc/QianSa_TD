# SD_to_Video 当前状态

更新时间：2026-09-18（当前源码，未生成 bitstream）

## 已完成修复

- `bmp_bilinear_scaler` 已修复奇偶行缓存推进落后一行的问题；行可用性检查和水平/垂直插值均已流水化，并增加 `abort` 恢复入口。
- `sd_media_pipeline` 已对 `write_req_ack` 做两级同步，保持 `write_finish` toggle CDC；BMP、缩放器和 SDRAM 两个完成事件任意先后都由保持型提交状态汇合。
- 加载失败会中止缩放器并释放媒体控制器，避免永久卡在加载态。
- 数码管新增阶段码：`A`=源 FIFO/扇区节流，`B`=BMP 已结束而缩放器仍在排空，`C`=等待 SDRAM 写完成，`D`=加载失败/中止。原 0–4 含义保留。
- `key2` 仍为手动下一张，`swkey2` 仍为自动播放电平开关；未修改顶层外部端口和管脚映射。
- 两个 PLL 的 reset 已接板级复位；50 MHz、100 MHz、125 MHz SDRAM、25 MHz 视频域分别使用异步置位、同步释放并保持 1024 个本地域周期的复位。
- FAT32/WAV 文件定位中的乘法和多级地址加法已拆拍，避免与图像链路共用的 100 MHz SD 域出现长组合路径。
- 板载图像参数调节已整合：SW3/SW4 选择亮度、对比度或锐度，KEY3/KEY4 以 5 为步长在 0–100 内调节，左上角 OSD 显示参数名或数值。
- 锐度使用两级视频流水的水平左邻域差分增强；每行首像素旁路，消隐期清除邻域状态，DE/VS 已同步延迟两拍。
- 旧 UART 亮度/音量块状状态条已移除；UART 命令对亮度和音量的实际控制保留。
- 按原理图核实引脚：KEY2/3/4 为 B2/B1/C1，SW1–4 为 C8/C7/C6/C5。
- BMP0/1/2 已固定预装到 SDRAM 槽0/1/2；首图提交后在视频帧边界显示，后两槽后台预装完成后切图不再访问 SD 卡。手动连按经“请求/视频帧边界已应用”双向 toggle 握手逐项显示，不跳图、不丢图。
- 对比度、锐利度已改为 0–100、步长 5 的 21 级 Q6 曲线，50 为原图；音频 FIFO 低于 1024 个立体声采样帧时，SD 仲裁器优先服务音频。

## 长时间静置后切图彩点：现场诊断

- 实板现象：连续切图流畅；若长时间保持在一张图，再切换到其他已预装图片，出现大量彩色异常像素，且异常持续存在而不会在数帧内自行恢复。
- 已排除：预装完成后媒体控制器不会重新从 SD 读取或覆写 ready 槽；异常持续存在，不符合切换瞬间的读 FIFO 残留、显示槽 toggle 丢失或短暂跨域采样造成的混帧特征。
- 帧槽容量已核对：槽 0/1/2 的起始地址分别为 `0`、`307200`、`614400` 个 32-bit word；三帧共 `921600` word（约 3.52 MiB），低于 21-bit 地址空间的 8 MiB，因此不是槽位重叠或地址越界。
- 已完成实板排除：C2 的三个额外相位版本仍复现同一现象。因此 DQ 相位裕量不是主要根因；保留当前 180°版本，不再继续扩大相位扫描。
- 当前最高优先级根因：预装策略使槽 1/2 在切换前可长期完全不访问，而旧策略在每次切图前都会从 SD 重写目标槽。显示中的槽持续被逐帧读取，读访问会反复激活其所在行；空闲槽只能依赖 SDRAM 自动刷新。故“旧版没有、新版长时间静置后才出现，且只在切到其他图片后持续有彩点”与空闲 SDRAM 行的保持/刷新链路失效高度一致。
- 接口与文档核对：工程使用 APUG011 的 ERAM 接口方案，而非 APUG010 的 Local Bus 方案。官方参考 `app_wrrd.v` 同样以 32-bit word 地址连续 `+1`，因此槽地址单位保持不变；工程中不存在 APUG010 所需的 `cs_l/we_l/oe_l/la/ld/Local_Busy` 接口。
- 已定位的外围协议缺口：`frame_fifo_read.v` 与 `frame_fifo_write.v` 虽接收 `Sdr_init_ref_vld` 和 `Sdr_busy`，但各自的 `S_CHECK_FIFO` 状态启动下一个 SDRAM burst 时没有判定这两个信号。APUG011 要求自动刷新模式在读写前确认刷新标志已撤销；否则新请求可能干扰刷新窗口。该缺口比 DQ 相位更符合现象，且无需修改加密 IP。
- 已实施受控手动刷新：`sdram_refresh_scheduler` 在 125 MHz SDRAM 域中拉高保持型 `App_ref_req`，并阻止读写 FIFO 启动新 burst；观察到 `Sdr_init_ref_vld` 上升沿后才释放。首版保护窗为 256 周期，实板显示 `0x0F51` 且超限 `E` 置位，证明刷新确认晚于 `0x0F42` 截止值 15 周期。工程实际 burst 为 256 word，原保护窗没有覆盖读数据尾部和状态机/控制器开销；现已扩大为 512 周期。`sdr_as_ram` 保持 `self_refresh_open=1'b0`。若到达刷新间隔仍无确认，诊断故障粘滞；即使迟到确认到达，也只释放 `App_ref_req` 而继续阻止新的用户访问，防止随机闪线继续传播。
- 刷新密度验证：公开的同芯片 EG4S20BG256 项目采用约 7.8 us 刷新并在整页 burst 前预留刷新时间；当前工程旧值 `3906` 周期为 31.25 us，和 APUG 源码“64 ms 内 4096 次”注释的 15.625 us 不一致。15.625 us 验证值 `1953` 周期已实板确认刷新页为 `0x06B0` 且无 `E`，但仍出现 `b=0000、d>0000` 的闪线；7.816 us 验证值 `977` 周期实板显示 `0x02E0` 且无 `E`，结论相同。用户已将 SDRAM 及其 180°相移时钟改为 100 MHz；为保持 25.0 us 实际间隔，受控手动刷新周期同步改为 `2500`，保护窗仍为 512 周期；`sdr_as_ram` 仍为手动刷新模式。
- 数码管诊断：前四位显示两次刷新确认之间的最大间隔（十六进制，高位在左），第五位 `E` 表示间隔超限，最后一位继续显示原加载阶段码；2500 周期版本在100 MHz下正常值必须不超过 `0x09C4`，且 `E` 不亮。实板仍须执行槽0显示至少10分钟、依序切至槽1/2各至少5分钟；若仍失败，应转为分离帧签名中的持续损坏与动态读取变化，再决定是否调整PLL/DQ采样。
- 512 周期保护窗、31.25 us 旧周期实板多次烧录后显示 `0x0E50` 或 `0x0E4B`，第五位不再显示 `E`；15.625 us 版本显示 `0x06B0` 且无 `E`，两者都证明刷新确认在各自截止前到达，但闪线仍在上电运行一段时间后出现。三槽均为 `b=0000`，且闪线出现后 `d` 快速增加。
- 已加入帧读诊断：接通异步读 FIFO 的 `empty_flag`、`valid` 与读侧占用量，在显示有效后分别粘滞统计 FIFO 空读和同槽原始帧的签名/有效像素数变化。数码管每 2 秒轮换三页：`hhhh [空白/E] S` 为刷新间隔，`cccc b S` 为 FIFO 空读次数，`cccc d S` 为帧数据变化次数；`b/d` 页第五位小数点点亮表示对应计数非零。

## 工程和时序约束

- `prj/timing.sdc` 已加入 `prj/SD_to_Video.al` 的 `constraint_1`，顺序 1；`pin.adc` 顺序 2。
- SDC 已删除不存在的 `u_audio_pll` 和错误的 video/HDMI exclusive 关系。
- TD 实际读取约束后能看到全部 6 个时钟：50 MHz `clk`、100 MHz `sd_card_clk`、125 MHz `ext_mem_clk`、125 MHz/180° `ext_mem_clk_sft`、25 MHz `video_clk`、125 MHz `hdmi_5x_clk`。
- 当前源码可以成功 analyze/elaborate/optimize gate，无 black box；新复位模块已被工程识别。

## 已通过仿真

- 2×2→4×4：四角、行末、帧末和输出反压通过。
- 640×480→640×480：输入/输出均为 307200 像素，逐行标识、首末行和 `done` 通过。
- 完整 `BMP -> FIFO -> scaler -> frame_commit` 通过；只有厂商 FIFO 未连接可选端口的已知警告。
- 媒体控制器：首帧、手动三槽循环、加载中排队和 SW2 自动播放通过；SW2 测试已消除 NBA 取样竞态。
- FAT32/WAV reader 回归通过。
- `image_controls` 回归通过：消抖、参数选择、加减步长、0/100 饱和和 SW=`11` 保持选择通过。
- `video_presentation` 回归通过：三种参数 OSD、1/2/3 位数值、超时隐藏、亮度/对比度饱和、平坦区域与阶跃边缘锐化、行首旁路和字幕优先级通过。
- `frame_fifo_refresh_gate` 回归通过：刷新有效、SDRAM busy 或两者同时有效时，读、写均不会启动新 burst；信号撤销后恢复启动。
- `sdram_refresh_scheduler` 回归通过：以 2500 周期刷新间隔、512 周期保护窗验证初始化完成后在提前窗口请求刷新、保持请求直到确认、确认后重新计时；延迟确认但未越过保护窗时不得误报超限，未确认时保持阻断并置位故障，迟到确认不得在故障后重新开放数据通路。
- `frame_read_monitor` 回归通过：同槽相同帧稳定，单像素变化、缺失/额外像素均可检出；每槽独立建立基线，切回已有槽复用原基线；FIFO 正常读不误报，停止返回数据后空读计数与粘滞标志有效。

## 时序现状（必须如实保留）

旧报告只分析 50 MHz 输入时钟，不能作为时序通过证据。加载真实 SDC 后，问题已从隐藏状态变为可见：

- 最近一次当前源码综合估算：100 MHz SD 域 WNS `-1.733 ns`、Fmax `85.230 MHz`；相比最初 `-4.075 ns`/`71.048 MHz` 已明显改善，但仍未闭合。
- 50 MHz、25 MHz 和 HDMI 125 MHz 域为正裕量。
- 125 MHz SDRAM 逻辑域综合估算约 `-2.293 ns`；180°相移硬宏 I/O 路径约 `-5.291 ns`。
- 最新一次当前源码完整 P&R：DQ 多周期约束精确命中写方向 32 条、读方向 32 条 `U3/sdram.dq[*]` 硬宏路径；未放宽普通 SDRAM、视频或 SD 时钟路径。全局 setup WNS `-0.270 ns`、TNS `-7.075 ns`、52 个端点；hold WNS `+0.004 ns`。
- 因此当前仍不能宣称满足“非例外 setup >= +0.5 ns、hold >= +0.1 ns”，也不能将 SDRAM 静置槽彩点视为已通过时序验证的问题。

## 用户下一步

1. 不要使用旧 bitstream。当前源码尚未由 Codex 生成 bitstream。
2. 在 TD GUI 中重新运行综合和布局布线，确认日志包含 `read_sdc ../../timing.sdc`，Clock Summary 必须显示上述 6 个时钟。
3. 在 100 MHz SD 域和厂商 SDRAM 硬宏时序均闭合前，不应把板级随机黑屏或静置槽彩点视为已经完全解决。
4. 已完成三个额外 C2 相位版本的实板测试且均复现彩点；保持 180°，不再继续扫描相位。确认数码管最大刷新间隔不超过 `0x3D1`、第五位 `E` 不亮后，再进行静置槽实板验证；若仍有闪线且 `b=0、d>0`，刷新频率已与同芯片参考对齐，应转为细分动态读变化和持续内容损坏。
5. 时序闭合后由用户生成并烧录 bitstream，连续上电/复位至少 10 次；再分别测试 `key2` 连切 10 次及 `swkey2` 自动轮播。
6. 上板验证 SW3/SW4=`00/01/10` 时 KEY3/KEY4 分别调节亮度/对比度/锐度，SW=`11` 保持上一选择；检查 OSD 与 HDMI 画面无偏移。
7. 若仍停滞，记录数码管码：`A/B/C/D` 可分别定位 SD/FIFO、缩放排空、SDRAM 完成或加载失败阶段。
8. 等待闪线出现后记录一轮三页诊断值：`b>0` 优先修复读 FIFO 欠载；`b=0,d>0` 转为 100 MHz SDRAM 验证 DQ 实时读稳定性；两者均为 0 则启用 raw bypass 检查视频处理和 HDMI 后级。

## Git 与工作区

- 已提交：`eaa2cdf Fix scaler row progression and media handshakes`。
- 已提交：`9de54f4 Add synchronized resets and real timing constraints`。
- 工作区内其他既有修改不得覆盖或清理。
