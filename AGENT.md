## 项目摘要
这是一个用安路TD基于Verilog语言的FPGA音画联动的音游项目
源码位于'src'，项目和ip核位于'prj',仿真测试位于'sim'

## 代码规范
### Verilog 规范

- 使用 Verilog-2001 语法，保持现有模块的代码风格。
- 模块、端口和信号使用小写 `snake_case`。
- 参数和宏使用大写 `UPPER_SNAKE_CASE`。
- 时序逻辑使用非阻塞赋值 `<=`，组合逻辑使用阻塞赋值 `=`。
- 明确信号位宽，避免无尺寸常量及隐式位宽转换。
- 组合逻辑必须覆盖所有分支，避免意外生成latch。
- 一个时序寄存器原则上只在一个 `always` 块中赋值。
- 不使用 `#delay`、`force` 等不可综合语句编写设计逻辑。

### 状态机与握手

- 状态机应提供默认赋值和明确的非法状态恢复路径。
- `valid`、`ready`、`req`、`done` 等信号的有效周期必须明确。
- 请求未被接收前，不得改变与请求关联的地址和数据。
- 对脉冲和持续电平信号进行明确区分，并在注释中说明。

### 文件与模块

- 一个主要模块对应一个同名 `.v` 文件。
- 可复用功能优先拆分为独立模块，避免在顶层堆积业务逻辑。
- 不修改 `src/include/*.enc.v`、厂商 IP 和自动生成文件。
- 测试代码放在 `src/test/`，不得加入板级综合文件集。
- 除非任务明确要求，不改变顶层端口、管脚约束和时钟配置。

### 注释

- 用中文注释，确保不生成乱码
- 注释重点说明设计意图、时序约束、单位和特殊边界条件。
- 地址常量应注明其用途和单位，例如字节地址或扇区地址。
- 修改代码时同步更新已失效的注释，不保留与实现冲突的描述。

### 修改原则

- 修改范围保持最小，避免无关重构和批量格式化。
- 修复缺陷时补充相应 testbench 或回归断言。
- 提交前检查综合告警、仿真结果和 `git diff`。

## 行为约束
- 如需生成新的IP核，应指导用户手动生成
- 不要随便修改顶层，如需改动应和用户说明
- 进行一次代码改动应在git上进行一次git commit
- 分配引脚应按照原理图中的引脚进行分配，不要按照参考例程，必须以当前项目原有设计及用户明确要求为准。
- 不生成bit流文件，由用户自己生成

## 测试
- 新增行为应增加对应仿真测试。
- 交付时说明修改内容、验证结果及遗留风险。

## 上下文与 Token 控制
- 限制构建日志输出：日志保存到 `prj/log/`，对话中只提取退出状态、错误、关键告警、WNS/TNS、Fmax 和首条关键路径，不输出整份构建日志。
- 减少完整综合与布局布线次数：修改前优先进行静态分析和局部仿真；相关修改集中完成后再构建；中间阶段原则上只运行综合，最终候选方案才运行布局布线并生成结果。
- 缩小文件读取范围：优先使用 `rg -n` 定位信号和实例，只读取相关代码附近的必要行；不读取完整大文件或完整报告，也不重复读取未发生变化的内容。
- 每轮只处理一个明确目标：将诊断、实现修复和最终验证分阶段进行，未经用户明确要求，不在同一轮中连续扩展到新的架构或时钟方案。
